# CLAUDE.md

Mọi quy tắc nằm ở **[AGENTS.md](./AGENTS.md)**. Đọc nó trước khi làm bất cứ gì.

Tài liệu sản phẩm gốc (single source of truth): `../context.html` — Orchestra Engineering Playbook.

Bổ sung riêng cho Claude Code:
- Chạy `pnpm turbo lint typecheck test` trước khi mở PR.
- Trước khi coi hạng mục là "xong" trong track Founder B: chạy 3 cổng `/sonle` → `/lky-institution-builder` → `/nhi feedback` (skill ở `.claude/skills/`), lưu kết quả vào `docs/gates/`.
- Không chạm folder ngoài slice của issue (xem AGENTS.md §2).
