# E: drive is exFAT (no symlink support) → hoisted linker required.
# Also lets tooling resolve deps flatly on this filesystem.
node-linker=hoisted
# Don't re-run `install` before every `pnpm run` (breaks on un-approved native builds).
verify-deps-before-run=false
