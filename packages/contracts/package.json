{
  "name": "@orchestra/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "[S] Shared — single source of truth for interfaces between domain A and B. Change = PR + both founders approve.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
